# 📜 简牍 V2.0 项目重要信息

> 版本：2.0.5-B（带反馈系统）  
> 最后更新：2026-04-26  
> **新会话必读：包含所有关键配置和启动信息**

---

## 🚀 快速启动（每次开机后）

### 方法 1: 桌面快捷方式
双击桌面上的 **"简牍服务器"** 快捷方式

### 方法 2: 手动启动
```powershell
cd "D:\简牍_V2.0\admin-server"
node server.js
```

**成功标志：**
```
✅ 数据库连接成功
✅ 默认管理员账户已创建
🚀 后台服务器运行中：http://localhost:3001
```

---

## 🔐 关键账户信息

### 默认管理员
- **用户名**: `admin`
- **密码**: `admin123`
- **角色**: `admin`（完全管理员）

### MySQL 数据库
- **主机**: `localhost:3306`
- **用户名**: `root`
- **密码**: `hc19970412`
- **数据库**: `jiandu_admin`

---

## 🌐 系统入口

| 页面 | URL | 权限要求 |
|------|-----|----------|
| **工具主页** | `http://localhost:3001/workspace-auth.html` | 所有登录用户 |
| **后台管理** | `http://localhost:3001/admin/index.html` | 管理员 + 意见收集员 |
| **反馈管理** | `http://localhost:3001/admin/feedback.html` | 管理员 + 意见收集员 |
| **经典版** | `http://localhost:3001/workspace.html` | 无需登录（不保存历史）|

---

## 👥 用户角色系统

### 三种角色

| 角色 | 权限 | 说明 |
|------|------|------|
| `admin` | 完全管理员 | 所有权限（用户管理 + 反馈管理 + 系统配置）|
| `feedback_collector` | 意见收集员 | 查看用户列表 + 管理反馈（不能修改/删除用户）|
| `user` | 普通用户 | 只能提交反馈和查看自己的反馈 |

### 设置意见收集员
1. 管理员登录后台管理
2. 找到目标用户 → 点击"编辑"
3. 将角色改为 `feedback_collector`
4. 保存

---

## 📁 核心文件结构

```
D:\简牍_V2.0\
├── admin-server/              # 后端服务器
│   ├── server.js              # 主服务器（含反馈 API）
│   ├── .env                   # 环境配置（数据库密码等）
│   └── database-schema.sql    # 数据库结构
│
├── admin/                     # 后台管理前端
│   ├── index.html             # 后台管理主页
│   └── feedback.html          # 反馈管理页面（新增）
│
├── workspace-auth.html        # 工具主页（登录版，带反馈入口）
├── workspace.html             # 工具主页（经典版，无需登录）
├── index.html                 # 项目首页
├── settings.html              # 设置页面
│
├── js/
│   ├── app.js                 # 经典版核心逻辑
│   └── app-enhanced.js        # 增强版（支持历史保存）
│
├── 启动服务器.bat             # 一键启动脚本
├── 项目重要信息.md            # 本文件（新会话必读）
└── 快速启动.md                # 使用指南
```

---

## 🎯 核心功能

### 1. 数据分析（工具主页）
- 上传 Excel/CSV 文件
- AI 自动生成 Python 代码分析数据
- 生成报告并下载
- 保存分析历史（登录版）

### 2. 用户系统（后台管理）
- 用户注册/登录
- 用户管理（增删改查）
- 角色权限管理
- 用户统计分析

### 3. 意见反馈系统（新增）
- 普通用户提交反馈
- 意见收集员管理反馈
- 反馈状态：待处理/处理中/已解决/已拒绝
- 管理员回复反馈

---

## 🔧 关键配置

### 服务器端口
- **HTTP 端口**: `3001`
- **MySQL 端口**: `3306`

### 环境变量（`admin-server/.env`）
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=hc19970412
DB_NAME=jiandu_admin
JWT_SECRET=jiandu_secret_key_2026_change_in_production
JWT_EXPIRES_IN=24h
PORT=3001
```

---

## 🐛 常见问题

### Q1: 重启电脑后无法访问
**解决**: 服务器不会自动启动，需要手动运行：
```powershell
cd "D:\简牍_V2.0\admin-server"
node server.js
```

### Q2: 端口 3001 被占用
**解决**: 修改 `.env` 中的 `PORT=3002`，然后重启服务器

### Q3: MySQL 连接失败
**解决**: 
1. 检查 MySQL 服务是否运行：`net start MySQL`
2. 确认 `.env` 中的密码正确：`hc19970412`

### Q4: 浏览器提示"网络不好"
**解决**: 
1. 确保服务器已启动
2. 清除浏览器缓存（`Ctrl + Shift + Delete`）
3. 强制刷新页面（`Ctrl + F5`）

---

## 📊 数据库表结构

### 核心表
- `users` - 用户表（含角色：admin/feedback_collector/user）
- `analysis_history` - 分析历史
- `user_feedback` - 用户反馈（新增）
- `operation_logs` - 操作日志
- `sessions` - 会话
- `uploaded_files` - 上传文件
- `user_preferences` - 用户偏好

---

## 🎉 新会话快速上手

如果您开启了新会话，只需告诉我：

1. **"启动服务器"** - 我会帮您启动后端服务
2. **"打开工具主页"** - 我会打开浏览器到工作空间
3. **"查看反馈"** - 我会打开反馈管理页面
4. **"设置意见收集员"** - 我会指导您升级用户角色

所有项目文件都在 `D:\简牍_V2.0\` 中，我可以随时读取和修改！

---

**🦞 龙虾工头提示：**
- 每次开机后记得先启动服务器
- 默认管理员密码 `admin123` 建议修改
- 意见收集员是很实用的角色，适合客服/产品团队
- 有问题随时问我，项目结构我都记着呢！

---

**文档版本**: 1.0  
**最后更新**: 2026-04-26  
**维护者**: 龙虾工头 🦞
